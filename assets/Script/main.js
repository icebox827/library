const myLibrary = [];

class Book {
  constructor (author, title, pages, read) {
    this.author = author;
    this.title = title;
    this.pages = pages;
    this.read = read;
  }

  setAuthor(author) {
    this.author = author;
  }

  setAuthor(title) {
    this.title = title;
  }

  setAuthor(pages) {
    this.pages = pages;
  }

  setAuthor(read) {
    this.read = read;
  }
}

function addBook(book) {

}